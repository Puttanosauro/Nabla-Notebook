package org.example.project.qdcore.media.export

actual fun downloadRemoteMediaBytes(url: String): ByteArray {
    // TODO (JVM): Download the bytes using java.net.URL(url).readBytes()
    return byteArrayOf(0)
}